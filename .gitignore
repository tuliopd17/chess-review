.venv/
__pycache__/
*.pyc
.DS_Store
.idea/
.vscode/
*.egg-info/
dist/
build/

# Binários do Stockfish WASM — baixados automaticamente (em runtime no dev, ou no
# build do Docker em produção). Não precisam ir pro Git (~11 MB).
backend/data/sf/

# Cache do índice de aberturas — gerado automaticamente (build do Docker/runtime).
backend/data/openings_index.json

# Logs locais
*.log
